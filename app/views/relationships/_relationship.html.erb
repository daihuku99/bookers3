
	<% if current_user.following?(user) %>
		<%= link_to 'フォローを外す', unfollow_path(user.id), method: :post, remote: true %>
	<% else %>
		<%= link_to 'フォローする', follow_path(user.id), method: :post, remote: true %>
	<% end %>
